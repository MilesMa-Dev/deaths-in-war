# No environment variables required for core features.
# Wikipedia scraping and all core features work out of the box.

# Optional: Secret for Edge Middleware → Backend crawler log ingestion
# Must match the same value set in Vercel environment variables
# Generate with: openssl rand -hex 32
CRAWLER_INGEST_SECRET=
