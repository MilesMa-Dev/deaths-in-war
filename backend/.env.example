# (Optional) ACLED API credentials for conflict event enrichment
# Register at https://acleddata.com/register/ to get credentials
# If not provided, the app still works — just without ACLED enrichment data
ACLED_EMAIL=
ACLED_PASSWORD=
